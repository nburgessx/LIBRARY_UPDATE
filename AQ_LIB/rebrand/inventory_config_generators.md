# 0.10 — config folder & generator inventory

`resources\config` — ships with the `AQ_API` and `AQ_XLL` packages; the
no-recompile customisation surface (calendars + generators).

## Contents

| Item | Count / notes |
|---|---|
| `SWAP_GENERATOR\*.JSON` | 132 |
| `CURVE_GENERATOR\*.JSON` | 116 |
| `BOND_GENERATOR\*.JSON` | 21 |
| `SwapGenerator.conf`, `CurveGenerator.conf`, `BondGenerator.conf` | per-type registries |
| `Calendar.csv` | wide matrix — columns = holiday-centre codes (`LnB`, `NYB`, `SyB`, `TYO`, `Tgt`, …), rows = holiday dates. Centre codes are short alphanumerics — **never contain `:`** (confirms accept-both is safe, see `calendar_delimiter_sites.md`). |
| `Calendar.conf` | calendar registry |
| `CBSchedule.csv` | central-bank meeting schedule |
| `HolidayCentres.xlsx` | source spreadsheet (MarketWire / SwapsWire import) |
| `calib.properties`, `ir.properties`, `startup.conf`, `irsvr_excel.conf` | runtime properties |
| `MLIBQ_ADDIN.xll` | **stale bundled old add-in binary — delete** |

## Generator JSON format

The AQObj cached-object serialisation format:

```json
{
  "CACHED_OBJECT_TYPE": "BOND_GENERATOR",
  "STRUCTURED_KEYS": [
    { "SK_NAME": "BOND_STATICDATA", "SK_NUMBER_OF_COLUMNS": 2,
      "SK_COLUMN_TYPES": ["STRING","STRING"],
      "COL_1": ["BondType","Currency","YieldType","IsCleanPrice","BondQuoteConvention"],
      "COL_2": ["FIXED","USD","YIELD-TO-MATURITY","TRUE","QUOTE_IN_64THS"] },
    { "SK_NAME": "BOND_SCHEDULE", ... }
  ],
  "NAME": "US_TREASURY_TYPE1"
}
```

`SWAP_GENERATOR` / `BOND_GENERATOR` are registered `CACHED_OBJECT_TYPE`s
(`etrading\include\Environment.h`, `ObjectUtilities.h`:
`functionTupleForLWO<SwapGenerator>()`), i.e. generators are AQObj/LWO objects.
`CommonConstants.h` defines `SWAP_GENERATOR` in **4 different namespaces** —
tidy during the rebrand.

## Rebrand / cleanup items

| Item | Action | Phase |
|---|---|---|
| Hard-coded client paths — `calibration\src\LACurveProperties.cpp:16-18` (`M:\02_Project\5858_mlibir\configration\...`) and `etrading\src\FolderConfig.cpp:28-31` (`S:\mizuho\fig\fi_rstac\mlibir\configration\...`) for `Calendar.csv` / `ir.properties` / `calib.properties` / `CBSchedule.csv` | Re-point the `DEFAULT_*_PATH` constants to the shipped `config` folder, resolved **relative to the loaded module / add-in** (not a drive letter). Functional, not cosmetic. | 1 / 4b |
| `MLIBQ_ADDIN.xll` in `config` | delete | 1 |
| Calendar fields in ~103 generator JSON using `"XX:YY"` | migrate to `"XX+YY"` (calendars only; curve-name lists keep `:`) | 4b.4 / 6.4 |
| Legacy names / conventions in generator JSON, `.conf`, `.properties` | audit & rebrand | 6.4 |
| `swig_R_wrap.cpp` (committed, compiled) has stale absolute paths in comments; the `*_wrap.cxx` are regenerated by `generate*` scripts | regenerate R wrap too; consider not committing generated wrappers | 1.2 / 5 |
| `config` packaged only by `deployPython_2022.bat` today | add `config` copy to the `AQ_XLL` post-build and the C#/Java/R deploy scripts | 4b.3 |

## Generator support to build (Phase 4b.2)

`Generators` category: `aqGeneratorsList`, `aqGeneratorsDescribe`,
`aqGeneratorsValidate`; per-asset `aqSwapsFromGenerator` / `aqBondsFromGenerator`
/ `aqCurvesFromGenerator` taking `generatorName` + the few trade-specific
overrides. Schema-validation `GOOGLE_TEST` over all 269 shipped JSON.
